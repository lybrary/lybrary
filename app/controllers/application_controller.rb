class ApplicationController < ActionController::Base
  # We're building an API so we don't want this on anything really.
  # We're also just prototyping so lets keep life simple.
  #protect_from_forgery with: :exception
end
